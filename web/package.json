{
  "name": "stock-analyst-web",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "종목분석 에이전트 — 웹 플랫폼 (Astro + Supabase + Vercel)",
  "scripts": {
    "predev": "node scripts/fetch_fear_greed.mjs && node scripts/build_manifest.mjs && node scripts/build_kb.mjs && node scripts/build_search_index.mjs",
    "dev": "astro dev",
    "prebuild": "node scripts/fetch_fear_greed.mjs && node scripts/build_manifest.mjs && node scripts/build_kb.mjs && node scripts/build_search_index.mjs && node scripts/set_password.mjs && node scripts/sync_portfolio.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.105.1",
    "astro": "^4.16.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
