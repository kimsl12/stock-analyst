# scripts/ 디렉토리 Python 의존성
# 설치: pip install -r scripts/requirements.txt
#
# (참고: fetch_price.py가 사용하는 yfinance/pykrx는 별도 관리.
#  이 파일은 sync_portfolio_to_supabase.py 전용.)

supabase>=2.0,<3.0
python-dotenv>=1.0,<2.0
