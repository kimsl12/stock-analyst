{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "git fetch --deepen 500 2>/dev/null || true; cd web && npm install --prefer-offline --no-audit --no-fund && npm run build",
  "outputDirectory": "web/dist",
  "installCommand": "echo skip-root-install",
  "framework": null,
  "functions": {
    "api/price/[ticker].py": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
